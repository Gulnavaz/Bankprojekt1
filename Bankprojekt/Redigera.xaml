<Page
    x:Class="Bankprojekt.Redigera"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bankprojekt"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="White" Margin="0,0,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="249*" />
            <ColumnDefinition Width="391*"/>
        </Grid.ColumnDefinitions>

        <ListView Margin="317,177,331,158" Name="CustomersListView" Grid.ColumnSpan="2" Background="Orange">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" >
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" />
                            <TextBlock Text="{Binding PNr}" FontStyle="Italic" FontSize="16" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <TextBlock Text="Redigera Kund" TextWrapping="Wrap" FontSize="66" Margin="415,46,374,515" Grid.ColumnSpan="2" Foreground="Orange"/>
        <Button Content="Redigera" Margin="317,597,0,0" VerticalAlignment="Top" Height="59" Width="132" Background="#33000000" FocusVisualPrimaryBrush="White"/>
        <Button Content="Insättning/uttag" Grid.Column="1" Margin="290,597,0,0" VerticalAlignment="Top" Height="59" Width="155" Background="#33000000" FocusVisualPrimaryBrush="White" Foreground="Black"/>

    </Grid>
</Page>
